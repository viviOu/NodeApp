NodeApp
=======

This is a basic Node app which can be used to run a web server with express 4 framework.

It contains all dependencies for express 4 as well as underscore and socket.io modules.

To start a project:

download all files and install dependencies.

      npm install
      
To run the app:

      node app.js
